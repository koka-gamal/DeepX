# Core training stack
torch>=2.3.0
transformers>=4.51.0
peft>=0.11.0
trl>=0.9.0
bitsandbytes>=0.43.0
accelerate>=0.30.0
datasets>=2.19.0

# Data
pandas>=2.2.0
openpyxl>=3.1.0

# Optional: faster attention (recommended for Qwen3)
# flash-attn>=2.5.0   # install separately: pip install flash-attn --no-build-isolation

# Optional: experiment tracking
# wandb
